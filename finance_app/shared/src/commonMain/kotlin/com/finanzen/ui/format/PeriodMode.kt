package com.finanzen.ui.format

/** Modo de filtro de periodo (mensual o anual), compartido entre las pantallas que filtran por
 * fecha (Resumen, Reportes) — antes cada una declaraba su propia copia de este enum. */
enum class PeriodMode { MONTH, YEAR }
