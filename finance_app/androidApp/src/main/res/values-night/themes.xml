<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Modo oscuro: parent oscuro + mismo windowBackground (que resuelve a su color -night). -->
    <style name="Theme.FinanZen" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
